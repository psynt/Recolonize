package application;
/**
 * Used this to create an inner class so i could use the outer. truly indigenous
 * @author nichita
 *
 * @param <T> type involved
 */
public interface Report <T>{

	void tell(T text);

}
